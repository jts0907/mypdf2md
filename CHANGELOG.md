# CHANGELOG — mypdf2md

PDF → 마크다운 변환기(Streamlit) 변경 이력.

---

## 2026-06-16 — UI 리디자인 · 성능 개선 · 버그 수정

변환 로직(`pdf_to_markdown`, `table_to_markdown`)의 **추출 알고리즘은 변경 없음**. UI/성능/안정성만 개선.

### Added (추가)
- **변환 캐싱** (`@st.cache_data`, `max_entries=50`)
  - `pdf_to_markdown`이 파일 **bytes를 입력**으로 받아 내용 기준 캐싱.
  - 같은 파일은 최초 1회만 변환, 이후 재실행(다운로드 클릭·익스팬더 토글 등)에서는 캐시 재사용.
- **ZIP 일괄 다운로드**
  - 변환 성공 **2건 이상**일 때만 하단에 `📦 전체 Markdown ZIP 다운로드` 버튼 노출.
  - ZIP 내부 파일명 충돌 시 `_2`, `_3` 접미사로 자동 분리.
  - 표준 라이브러리 `zipfile` 사용 → `requirements.txt` 변경 불필요.
- **`.streamlit/config.toml`** 테마 추가(네이비 계열 기본 위젯 색 통일). *선택 사항.*

### Changed (변경)
- **UI 리디자인**
  - Pretendard 폰트 적용(CDN, 차단 시 시스템 폰트 폴백).
  - `layout="wide"`에서 본문 폭 `1080px` 제한 → 산만함 제거.
  - 좌측 네이비 액센트 바가 있는 헤더 밴드.
  - 안내 박스의 `display:inline-block` 제거 → 폭 정상화.
  - "사용 방법"을 `st.expander`(기본 닫힘)로 전환 → 초기 화면 경량화.
  - 다운로드 버튼 네이비 강조, 상단 햄버거 메뉴·기본 푸터 숨김.
- 파일 핸들 읽기를 `uploaded_file.read()` → **`uploaded_file.getvalue()`** 로 변경.

### Fixed (수정)
- **`.read()` 포인터 소모 버그**: 재실행 시 포인터가 끝에 있어 빈 결과가 날 수 있던 문제를 `getvalue()`로 해소(캐싱 도입과 함께 처리).
- **`DuplicateWidgetID` 오류**: 위젯 key를 **파일명 → 루프 인덱스(`idx`)** 로 변경.
  - 증상: 이미 올린 파일과 **같은 이름**의 파일을 추가 업로드하면 두 번째 항목에서
    `"변환 중 오류가 발생했습니다..."`(실제로는 PDF와 무관한 메시지) 발생.
  - 원인: `key=f"textarea_{파일명}"` / `key=f"download_{파일명}"` 가 동일 파일명에서 중복 생성되어
    Streamlit이 예외를 던졌고, 이를 `except Exception`이 잡아 일반 오류 메시지로 표시.
  - 해결: `key=f"textarea_{idx}"`, `key=f"download_{idx}"` (인덱스는 항상 고유).

### Notes (참고)
- 캐시는 앱 재시작·재배포 시 초기화됨(정상).
- 같은 파일을 두 번 업로드하면 결과 블록이 두 번 표시됨(의도된 동작). 중복 차단이 필요하면
  파일명+크기 기준 dedupe를 별도 구현해야 함.
- 변경 후 권장 점검: ① 복수 파일 변환 ② ZIP 다운로드 ③ **동일 파일명 재업로드 시 정상 처리**.

### 향후 후보 (미반영)
- **통합 단일 `.md` 병합 출력** — 여러 PDF를 구분선과 함께 하나의 마크다운으로(예: Dify 지식베이스 투입용).
- **진행률 표시**(`st.progress`) — 다량 변환 시 처리 현황 가시화.
- **변환 품질 개선** *(범위 큼, 샘플 PDF 선검토 필요)*
  - 2단 편집 법령/관보 PDF에서 좌우 칼럼 혼입 가능성(좌표 기반 줄 재구성의 한계).
  - 조문 구조화: `제N조` / `①` / `1.` 패턴을 헤딩·리스트로 승격.

---

*도구: pdfplumber · 제작: eonow687*
